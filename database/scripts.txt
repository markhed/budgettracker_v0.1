//Allotment
CREATE TABLE ALLTMNT (
ID INT NOT NULL,
CURAMT INT,
TGTAMT INT,
TGTDATE DATE
);

//BudgetAccount
CREATE TABLE BGTACCNT(
ID INT NOT NULL,
MANAGERID INT,
CURAMT INT,
TITLE VARCHAR(30),
ACCNUM VARCHAR(4),
STATUS VARCHAR(1)
);

//BudgetCycle
CREATE TABLE BGTCYCLE (
ID INT NOT NULL,
MANAGERID INT,
DAILYOUTID INT,
DISPAMT INT,
TTLREMOUT INT,
TTLINCOUT INT,
TTLACTIN INT,
STRTDATE DATE,
ENDDATE DATE,
STATUSCD VARCHAR(1)
);

//BudgetManager
CREATE TABLE BGTMNGR (
ID INT NOT NULL,
CASHHAND INT
);

//CreditCard
CREATE TABLE CCARD (
ID INT NOT NULL,
BANK VARCHAR(30),
CUTDATE DATE,
DUEDATE DATE,
ACCNT INT,
CLIMIT INT
);

//DailyOutflow
CREATE TABLE DOUTFLW (
ID INT NOT NULL,
MANAGERID INT,
TITLE VARCHAR(30),
COMMENT VARCHAR(255),
CRTNDATE DATE,
UNITAMT INT,
TTLUNIT INT
);

//Inflow
CREATE TABLE INFLOW (
ID INT,
BGTCYCLEID INT,
BGTTYPE VARCHAR(1),
GROUPID INT,
OUTAMT INT,
TITLE VARCHAR(30),
COMMENT VARCHAR(255),
CRTNDATE DATE,
INTYPE VARCHAR(1),
STATUSCD VARCHAR(1),
RECAMT INT,
PRDMASTERID INT,
SUBTYPE VARCHAR(1)
);

//Outflow
CREATE TABLE OUTFLOW (
ID INT NOT NULL,
TITLE VARCHAR(30),
OUTAMT INT,
UNITAMT INT,
TTLUNIT INT,
OUTUNIT INT,
BGTCYCLEID INT,
BGTTYPE VARCHAR(1),
OUTTYPE VARCHAR(1),
COMMENT VARCHAR(255),
CRTNDATE DATE,
GROUPID INT,
PRDMASTERID INT,
STATUSCD VARCHAR(1),
SUBTYPE VARCHAR(1)
);

//Owner
CREATE TABLE OWNER(
ID INT NOT NULL,
TITLE VARCHAR(30),
COMMENT VARCHAR(255),
MANAGERID INT,
SUBTYPE VARCHAR(1)
);

//Party
CREATE TABLE PARTY (
ID INT NOT NULL,
FNAME VARCHAR(30),
LNAME VARCHAR(30)
);

//Periodic Inflow
CREATE TABLE PRDINFLOW (
ID INT NOT NULL,
STDATE INT,
SPDAY INT,
SPDATE INT,
PERIOD VARCHAR(10),
MANAGERID INT
);

//Periodic Outflow
CREATE TABLE PRDOUTFLOW (
ID INT NOT NULL,
STDATE INT,
SPDAY INT,
SPDATE INT,
PERIOD VARCHAR(10),
MANAGERID
);

//User
CREATE TABLE USR (
ID INT NOT NULL,
ACCLOGIN VARCHAR(10),
PASSWORD VARCHAR(10),
FNAME VARCHAR(15),
LNAME VARCHAR(15),
EMAIL VARCHAR(30)
);